sudo: required

env:
  global:
  - IDCODE=$TRAVIS_BUILD_DIR/
  - IDprediction=pwd>>$TRAVIS_BUILD_DIR/filelocation.txt && $TRAVIS_BUILD_DIR/Run.sh

language: python

cache: pip

before_install:
  - sudo apt-get install build-essential
  - sudo apt-get install python3-pip
  - sudo pip3 install --user rdkit-pypi
  - sudo pip3 install --user numpy
  - sudo pip3 install --user scipy
  - sudo pip3 install --user matplotlib
  - sudo pip3 install --user pandas
  - sudo pip3 install --user regex
  - sudo pip3 install --user collections-extended
  - sudo pip3 install --user random2
  - sudo pip3 install --user seaborn
  - sudo pip3 install --user scikit-learn
  - sudo pip3 install --user coveralls
  - sudo pip3 install --user codecov
  - sudo pip3 install --user latex
  - sudo apt-get install -y texlive texlive-latex-extra texlive-latex-recommended
  
python:
  - "3.8"
  
language: shell

script:
  - chmod +x INSTALL.sh
  - chmod +x Run.sh
  - $TRAVIS_BUILD_DIR/Run.sh -b CCC
  - $IDprediction -c .1 -t trainset.csv

after_success:
  - coveralls
  - bash <(curl -s https://codecov.io/bash)
  - codecov
